require 'dotenv/load'


default_platform(:ios)

platform :ios do
  lane : my_lane do
    puts "Inside the lane: #{ENV["ENV"]}"
  end

#   lane :deploy do
#     # Actions to deploy the iOS app
#   end
end
